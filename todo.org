* enlarge reader font
* extract console
** add log
*** execute in gui thread
* extract reader
** add quit cmd
* ---
* add backup command
** backup folder setting, default /backup
** run tar -z proc
** store as db_timestamp.tar.gz
